#### TESTING-RELATED PACKAGES ####

# Checks style, syntax, and other useful errors.
flake8

# Static type checking
mypy==0.761

# Automatic code formatting
black

# Automatic import formatting
# We need to use the "develop" branch, not the latest stable release, so we have:
#   * https://github.com/timothycrosley/isort/pull/1000
#   * It can read from pyproject.toml.
#   * Some bug fixes.
git+https://github.com/timothycrosley/isort@a06e3d3#egg=isort[pyproject]

# For creating git precommit hooks
pre-commit==2.0.1

# Allows generation of coverage reports with pytest.
pytest-cov

# Allows codecov to generate coverage reports
coverage
codecov

# Optional dependencies, which we install for testing purposes.
matplotlib>=2.2.3

# Required to run sanic tests
aiohttp

# Required to run plugin tests.
pip>=20.0.1

#### DOC-RELATED PACKAGES ####

# YAML manipulation
ruamel.yaml

mathy_pydoc>=0.6.7,<0.7.0
markdown-include==0.5.1
# Package for the material theme for mkdocs
mkdocs-material==4.6.2

#### PACKAGE-UPLOAD PACKAGES ####

# Pypi uploads
twine>=1.11.0
